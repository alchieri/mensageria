package com.br.alchieri.consulting.mensageria.model.enums;

public enum BillingType {
    MONTHLY,
    PAY_PER_USE, // Pagar por uso (sem mensalidade fixa, apenas por mensagem/template)
    PREPAID      // Créditos pré-pagos
}
